version: 2

models:
  - name: TennisGoat_details_core
    description: "Model"

    columns:
      - name: "Winner"
        description: '{{ doc("TennisGoat_Winner") }}'
        tests:
          - is_adam_winner

      - name: "Runner"
        description: '{{ doc("TennisGoat_Runner") }}'

      - name: "Tournament"
        description: '{{ doc("TennisGoat_Tournament") }}'
        
      - name: "Year"
        description: '{{ doc("TennisGoat_Year") }}'
 